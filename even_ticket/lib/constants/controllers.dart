import 'package:even_ticket/controllers/menu_controller.dart';
import 'package:even_ticket/controllers/navigation_controller.dart';

// Controller instance of the menu items in the side bar
MenuController menuController = MenuController.instance;
// Controller instance for the navigation of the app
LocalNavController localNavController = LocalNavController.instance;
// Controller instance for the navigation of the login pages
LoginNavController loginNavController = LoginNavController.instance;

